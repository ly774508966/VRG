using UnityEngine;
using System.Collections;

public class Script3DMenu : MonoBehaviour {
	/*
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	 //OnSelected(true);
	//	yield return new WaitForSeconds(1);
	//	OnSelected(false);
	}
	
	void OnSelected(bool on){
		if(on){
			iTween.MoveTo(gameObject,Vector3(0,0,-1),0.5);
		} else {
			{
				iTween.MoveTo(gameObject,Vector3(0,0,0),0.5);
			}
	}
	*/
}
	